const devConfig = {
    apiUrl: process.env.NEXT_PUBLIC_API_BASE_URL || "webperpus-production.up.railway.app",
    frontendUrl: process.env.NEXT_PUBLIC_FRONTEND_URL || "https://https://perpustakaan-unsrat.vercel.app",
  };
  
  export default devConfig;
  